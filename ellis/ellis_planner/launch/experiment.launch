<?xml version="1.0"?>
<launch>
  <!-- Load experiment parameters. -->
  <rosparam command="load" file="$(find ellis_planner)/config/experiment.yaml" />

  <!-- Record data to ROS bag. -->
  <!-- TODO(eratner) Implement this... -->
  <!-- <node pkg="rosbag" type="record" name="rosbag_recorder" -->
  <!--       args="record -o /tmp/ellis-experiment /tf /mob/ellis_planner/vis /mob/choreographer/segment" /> -->

  <!-- Run the experiment manager. -->
  <node name="experiment_manager" type="exp" pkg="ellis_planner" output="screen" />
</launch>
